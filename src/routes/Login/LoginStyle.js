import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        marginBottom: 15,
    },
    button: {
        justifyContent: 'center',
        alignSelf: 'center',
        color: 'blue',
        fontWeight: '600',
        fontSize: 15,
        marginTop: 50,
    },
});

export { styles };
