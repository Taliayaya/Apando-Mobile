import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        width: 300,
    },
    inputContainer: {
        marginBottom: 20,
    },
    error: {
        textAlign: 'center',
        height: 17.5,
    },
});

export { styles };
