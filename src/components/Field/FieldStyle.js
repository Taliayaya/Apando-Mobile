import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        width: 300,
        height: 40,
        paddingHorizontal: 5,
        marginBottom: 5,
        backgroundColor: '#fff',
        borderRadius: 20,
        fontSize: 17,
        paddingLeft: 15,
    },
    inputContainer: {
        marginBottom: 20,
    },
    error: {
        textAlign: 'center',
        height: 17.5,
    },
});

export { styles };
